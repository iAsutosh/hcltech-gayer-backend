name: Validate Code

on:
 pull_request:
  branchs-ignored:
   - 'main'

jobs:
 validate:
  runs-on : ubuntu-latest

  steps:
   - uses: actions/checkout@v1

   - uses: actions/setup-node@1
     with: 
     node-version: 20.x

   - name: install dependecies
     run: npm install
    
   - name: lint check

   - name: package audit
  
   - name: test check
     run: npm run test

   - 
    
  

